---
"@omnirun/sdk": minor
---

Add LLM proxy client with streaming support. New `LLM` class provides `chatCompletion()`, `streamChatCompletion()`, `listModels()`, and `getUsage()` methods for the OpenAI-compatible LLM proxy endpoint.
